

export global Theme {
    // Base colors
    in property <color> app-background: #111827;      
    in property <color> panel-background: #1f2937;    
    in property <color> card-background: #111827;     
    in property <color> chart-background: #374151;

    // Text
    in property <color> text-primary: #f9fafb;
    in property <color> text-muted: #9ca3af;

    // Accent
    in property <color> primary-color: #2563eb;      

    // Controls
    in property <length> control-height: 32px;
    in property <length> control-radius: 6px;
}


